# Shell scripts must always use LF — CRLF breaks `set -o pipefail` under bash on the VPS.
*.sh text eol=lf
# Deploy/config files executed on Linux hosts
*.yml text eol=lf
*.yaml text eol=lf
Dockerfile text eol=lf
*.conf text eol=lf
